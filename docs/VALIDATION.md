# Validation

Run:

```bash
npm install
npm run validate
npm run android:apk
```

Validation checks the native project structure, runtime configuration, TypeScript, lint rules, and prohibited dependency references. Test login, account creation, OTP, all five tabs, vehicle lock authentication, notification permission, maps, web content, and deep screen back navigation on Android API 26 and a current Android version.
